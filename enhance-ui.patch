diff --git a/src/duc/cmd-cgi.c b/src/duc/cmd-cgi.c
index 7cd3f64..0b7f9ba 100644
--- a/src/duc/cmd-cgi.c
+++ b/src/duc/cmd-cgi.c
@@ -376,6 +376,8 @@ static void do_index(duc *duc, duc_graph *graph, duc_dir *dir)
 	}
 	printf(" </table>\n");
 
+	printf(" <div id=path>%s</div>\n", duc_dir_get_path(dir));
+
 	if(path) {
 		printf("<div id=graph>\n");
 		duc_graph_draw(graph, dir);
diff --git a/src/libduc-graph/graph.c b/src/libduc-graph/graph.c
index 4ef57c5..af4227b 100644
--- a/src/libduc-graph/graph.c
+++ b/src/libduc-graph/graph.c
@@ -447,9 +447,9 @@ int duc_graph_draw(duc_graph *g, duc_dir *dir)
 	}
 	
 	char *p = duc_dir_get_path(dir);
-	if(g->backend)
+	/*if(g->backend)
 		g->backend->draw_text(g, (int)g->cx, 10, FONT_SIZE_LABEL * g->font_scale, p);
-	free(p);
+	free(p);*/
 
 	struct duc_size size;
 	duc_dir_get_size(dir, &size);
