diff --git a/src/libduc-graph/graph-cairo.c b/src/libduc-graph/graph-cairo.c
index 3cce019..85ce600 100644
--- a/src/libduc-graph/graph-cairo.c
+++ b/src/libduc-graph/graph-cairo.c
@@ -115,7 +115,7 @@ static void br_cairo_draw_text(duc_graph *g, double x, double y, double size, ch
 
 	cairo_set_line_join (cr, CAIRO_LINE_JOIN_BEVEL);
 	cairo_set_source_rgba(cr, 1, 1, 1, 0.6);
-	cairo_set_line_width(cr, 3);
+	cairo_set_line_width(cr, 5);
 	cairo_stroke_preserve(cr);
 
 	/* black text */
diff --git a/src/libduc-graph/graph-html.c b/src/libduc-graph/graph-html.c
index 2aea85a..816a788 100644
--- a/src/libduc-graph/graph-html.c
+++ b/src/libduc-graph/graph-html.c
@@ -81,7 +81,7 @@ void br_html_start(duc_graph *g)
 
 	fprintf(f, "function t(text, s, x, y) {\n");
 	fprintf(f, "  c.font = s + 'pt Arial'\n");
-	fprintf(f, "  c.lineWidth = 2;\n");
+	fprintf(f, "  c.lineWidth = 4;\n");
 	fprintf(f, "  c.strokeStyle = '#ffffff';\n");
 	fprintf(f, "  c.fillStyle = '#000000';\n");
 	fprintf(f, "  var h = Math.floor(c.measureText('M').width * 1.6);\n");
